<!-- 通用框架入口：每次工作先读取 用户定制规则/防漂移总规则.md（相对技能包根目录），再读取适用协议和专项规则。 -->
# AI 视频项目框架｜先读我

画面可见文字按 用户定制规则/画面可见文字固定格式.md（R021）执行：英文提示词中用英文双引号保留原文和标点，不擅自翻译或改写。

人声、对白与说话者编号按 用户定制规则/人声对白与画面文字.md（R022）执行：编号按实际发声顺序稳定分配；对白原文保留；旁白、跨镜、截断和听不清内容使用固定标记。

辅助技能默认不使用。每次调用必须先执行 用户定制规则/辅助技能每次明文启用.md（R015）：取得用户针对本次调用的明文使用指令；安装、查看、历史、预设或推荐均不自动激活，融合层不得绕过该门槛。

用户说“更新框架”或“升级框架”执行同一路径：按 用户定制规则/框架升级与同步.md（R012）重新从磁盘读取本文件、活动规则、变更日志及适用插件登记，核验并加载，保存本 Agent 加载记录；不再询问方式。“本地更新／本地升级”同义。只有确认本地入口找不到才转远程。

启用辅助技能时先按 用户定制规则/辅助技能融合规则.md（R013）确定全部或部分使用范围；再按 用户定制规则/辅助技能扩展机制.md 展示并执行选中范围及必要依赖的规定流程、交用户裁定冲突，将有效结果融合到上一步版本并记录范围与前后版本。

创作入口必须先执行 用户定制规则/故事先审后选模型.md：先展示通用文字故事，用户通过后再选择模型并调用对应主 Skill 生成基础提示词。

每次远端发布必须执行 用户定制规则/首页更新记录规则.md，同步更新根目录 README.md 的说明和倒序更新记录。

辅助技能的安装、选择、引导、归档和跨 Agent 恢复按 用户定制规则/辅助技能扩展机制.md 执行；登记表位于 辅助技能/技能登记表.json。安装不等于本条提示词启用。

在明确指本框架的上下文中，“更新”和“升级”同义；对象不明时只澄清对象。明确“远程更新框架／远程升级框架”才直接获取远程版本；“同步远端”仍表示发布。

生成提示词按 用户定制规则/提示词优先交付.md 执行：必要检查后先展示，再做保存、索引及已授权归档。

获取新版框架或修改发布时，读取 用户定制规则/框架升级与同步.md；区分加载已发布规则与自行修改规则，核验后再激活。

任何可能离开当前视频编辑状态的操作，必须先读取并执行 用户定制规则/离开编辑状态强制落盘.md；包括列出历史、回看、切换、开始其他工作、暂停或结束。保存并读回核验成功后才能离开。

所有 Agent 首先读取 用户定制规则/硬规则锁定清单.md。清单及其中列出的官方技能、续写冻结基准只能读取，绝对禁止修改。

按该文件的编辑锁判断是否有新改动：已锁定且无新编辑时不重复落盘；发生新编辑后再保存。

每份 H3 提示词都必须有对应剧情版本；回看、修改和恢复按 用户定制规则/提示词与剧情双版本规则.md 执行。

多智能体或换机器接手时，读取 用户定制规则/多智能体协同规则.md，按统一事实来源和编辑锁交接，不依赖聊天记忆。

同一项目涉及多个生视频工具时，读取 用户定制规则/多引擎提示词归档规则.md，分离共同项目层与各引擎提示词层。

项目内视频与可选分镜按 用户定制规则/视频剧情与分镜记录规则.md 管理；未生成分镜时不虚构记录。

规则恢复、列出新增规则和回退按 用户定制规则/规则版本与回退控制.md 执行，并读取当前活动规则索引。

用户说“Agent设定”时，按 用户定制规则/Agent设定入口.md 进入框架与规则规划状态，不启动视频任务。

只有 Agent 设定明确开启期间才允许修改或回退规则；未开启时规则请求只记录待议。

用户选择续写后，必须先读取 用户定制规则/续写类型分流规则.md，确认物理延续或逻辑延续；只有物理延续启用 Motion Context 与冻结首镜规则。

启动顺序以 用户定制规则/项目地址启动入口.md 为准：先确认项目地址，再确认新视频/续写和预设。本文后续旧有“第一问新视频/续写”顺序由此替代。

归档视频项目时必须读取 用户定制规则/项目归档与复现.md，把预设快照、实际提示词、素材及已知依赖保存到具体项目，核验并列明缺失项。

查看或选择预设、查看或切换视频项目时，读取 用户定制规则/预设与项目列表.md，列出真实记录并支持数字选择；查看本身不改变状态。

用户说“列出历史”时，先执行离开编辑状态强制落盘，再逐项询问范围：1. 当前项目；2. 其他项目。

多视频管理必须读取 用户定制规则/多视频切换规则.md。中途创建其他视频前保存当前状态；返回已有视频恢复其独立记录，不重置设定、不混入其他视频内容。

你正在使用用户定制的 H3 提示词技能包。读完本文件后，按下面的入口自行读取所需文件，无需用户重复粘贴规则。所有路径均相对于本文件所在目录，移动文件夹后仍按相对路径解析。

## 必须读取

1. 用户定制规则/H3定制输出协议.md —— 完整工作流程与输出协议。
2. 用户定制规则/硬规则锁定清单.md —— 不可修改文件与 Agent 保护闸门。
3. 用户定制规则/离开编辑状态强制落盘.md —— 离开当前编辑状态前的保存闸门。
4. 用户定制规则/提示词与剧情双版本规则.md —— 提示词和剧情的对应、修改、回看与恢复。
5. 用户定制规则/多智能体协同规则.md —— 跨智能体、跨机器统一状态与交接。
6. 用户定制规则/多引擎提示词归档规则.md —— 同项目多工具分支归档与协同。
7. 用户定制规则/视频剧情与分镜记录规则.md —— 视频独立剧情及可选分镜记录。
8. h3-prompt-writing/SKILL.md —— 冻结的官方技能。根据任务模式读取其中指向的 references 文件。
9. 用户定制规则/声音上传模式标注.md —— 每条声音上传条目必须标明模式。
10. 当前为参考声线时，读取 用户定制规则/参考声线固定句式.md 和 用户定制规则/参考声音上传提示.md；当前为声音驱动时，读取 用户定制规则/声音驱动规则.md。用户切换模式时重新应用对应规则，不覆盖另一模式规则。

11. 用户定制规则/视频内容与写法隔离.md（R016） —— 视频创作与修改时必须读取：修改只影响当前视频；新视频不得参考、学习或沿用其他视频提示词的写法与内容，只应用本次输入和已确认预设字段。明确续写只继承授权衔接事实，不继承旧提示词写法。
12. 用户定制规则/H3每次询问输出格式.md（R017） —— H3 提示词生成前必须读取：每次单独确认输出格式，多参优先推荐但不自动选定；有无素材都询问。本次请求已明确格式则回显确认。确认后才按对应官方指南生成。

## 使用原则

- h3-prompt-writing/ 为官方原版，只读使用，不修改、不覆盖、不自动升级。
- 用户定制规则在官方文件之外约束任务。针对中文先审、两块交付等明确要求，按定制协议执行；不因此改写官方文件。
- 包内文档出现原电脑 E:\H3的技能 的路径时，指本文件所在根目录；原测试素材的绝对路径不能视为在当前电脑存在。
- 新任务先确认剧情继续还是新剧情，每次只问一项，尽量用数字选择。用户已明确给出的信息回显确认，不重复索要。
- 固定音频句式逐字执行，仅替换编号；不因模式切换、重新生成或语言润色改变。
- 最终交付为两个独立文本可拷贝块：素材上传顺序、H3 提示词。先中文审核，明确通过后才给英文；禁止代码块。
- 若当前平台原生支持 writing block，使用其渲染方式；不支持时优先采用平台原生的独立可复制文本容器。若无法满足，简短说明平台限制，不把不支持的标记当成已成功显示的文本块，也不改用代码块。
- 用户对本次内容的修改只更新本次状态；只有明确要求修改通用规则时才保存到用户定制规则。仅覆盖直接冲突项，其余保持。

## 继续已有测试与新任务

只有用户明确要求继续本包中已有测试时，才读取 用户定制规则/当前测试状态.md 并恢复状态。
新任务不自动继承其中的剧情、人物、音频模式、时长和画幅。
命名预设机制已启用，必须读取 用户定制规则/命名预设与本次覆盖.md；具体活动预设以预设库记录为准，旧方案仅作历史记录。

## 首次响应

读取上述必要文件后，直接按规则处理用户当前需求，不要求用户重新解释本说明。
若用户只是要求启用本包，简短告知规则已读取，然后问第一项：本次是剧情继续还是新剧情，提供数字选项。
若某必需文件确实无法读取，准确说明缺少的文件，不声称已加载。

## 续写入口补充
用户选择续写视频时必须读取 用户定制规则/续写视频首镜衔接规则.md。起始选项统一为1. 新视频；2. 续写视频。不强制上传上一段视频，以现有信息编写首镜连续动作。

## 续写冻结基准：与官方技能同级保护

续写时必须读取 续写衔接冻结基准-v1/README.md 与 续写衔接冻结基准-v1/已确认续写理解与时间规则.md。
这两份文件及来源校验记录，与官方技能享有相同的不可改写、不可覆盖、不可删除、不可自动更新保护。用户定制规则/续写视频首镜衔接规则.md 也是同内容的只读规则，不得继续追加改写。
这里的同级指用户项目内的保护与执行基准，不能覆盖系统约束，也不把第三方说明称为 MiniMax 官方文件。
后续新增理解或规则只能另存补充或新版本，用户明确确认后才生效，仍保留 v1 原件。遇到两份基准具体冲突先说明，不擅自修改任一原件。



预设相关任务必须读取 用户定制规则/命名预设与本次覆盖.md；六项已确认活动预设持续沿用，临时改动仅限当前视频。

每次完整交付必须读取 用户定制规则/素材顺序设定首行.md：第一块首行写明沿用的预设或本次已确认设定及有效六项，随后排列素材。


辅助技能原包保护按 用户定制规则/辅助技能原包保护与融合层.md（R014，相对框架根目录）执行：已安装原包不可修改；后续通用行为调整只新增独立融合技能版本，绑定固定原包并记录用户裁定。不得修改宿主源技能后回灌绕过保护。




视频创作与修改的"出稿前自检"按 用户定制规则/视频内容与写法隔离执行清单.md（R017，相对框架根目录）执行：交付前逐项跑三层任务类型 + 8 项自检；解决 attribute_transfer / fully_preserved / 11 节链路动作事件声链 / 短括号范围区间等写法漂移污染问题。R017 是 R016 的配套执行形态，R016 原件不动。





用户说框架版本、查询框架版本或查看框架版本时，执行 用户定制规则/框架版本查询.md（R018，相对框架根目录）。查询本地活动版本、基线、变化及可核实加载和同步状态；默认不联网，明确查远端才只读核验；不升级、不回退、不发布。

短指令入口（R019）：ver 查询框架版本，执行 用户定制规则/框架版本查询.md；list 列出框架指令及解释，执行 用户定制规则/框架指令查询.md。二者均为只读查询，不当作操作系统命令执行。

项目角色资产目录的创建、查看、整理与场景关联，执行 用户定制规则/项目资产管理.md（R023）：人物资产、公共资产、场景资产三大入口；具体人物目录扁平，包含人物、专属道具和声音，场景以链接关联。跨引擎共用真实资产，不自动重整其他项目。


Wan3主技能入口：wan3-prompt-writing/SKILL.md。调用前必须执行 用户定制规则/Wan3主技能冻结保护.md（R024）；Wan3入口、原始手册和来源校验与H3同级永久冻结，普通规则回退不解除保护。目标模型为Wan3时读取其主技能，不按辅助技能启用流程处理。本地入口与用户提供手册的来源分别标注。

用户选择Wan3输出提示词时，还必须读取 用户定制规则/Wan3定制输出协议.md；该协议属于框架级输出规则，项目只读取其本地只读副本。


项目地址确定后，优先读取该项目根目录的 先读我.md 及其引用规则，再进行项目操作，执行 用户定制规则/项目先读我与项目级更新.md（R025）。新增“项目级更新/更新项目规则”重新加载当前项目本地入口；项目规则只保留在项目内，不随框架发布远端。

项目规则不得与全局规则对冲或静默覆盖；发现冲突时暂停受影响操作，明文列出差异并交由用户选择，未选择前不执行冲突部分。执行依据：用户定制规则/项目先读我与项目级更新.md。

在确认项目目录后，用户输入“项目初始化”时，按框架项目规则创建项目骨架、先读我.md及索引；缺失才创建，保留已有文件，不生成具体资产、剧情或提示词。执行依据：用户定制规则/项目先读我与项目级更新.md。

项目初始化还必须完整读取框架内所有.md。框架内的输出协议属于框架级规则，项目只在`工作记录/框架规则副本/`保存适用协议的本地只读副本；框架更新后同步这些副本，项目副本不得反向定义或改写框架，项目规则不随框架发布。

提示词生成前只使用文案中的人物和场景名称；进入提示词阶段后先给素材上传块，再给提示词文字块，按 用户定制规则/模型资产占位符交付规则.md（R027）使用各模型占位符。

硬规定：每次提示词交付必须分成两个不同的文本块。第一块只能是素材上传顺序，第二块只能是提示词区；两块均须独立可拷贝，禁止合并、嵌套或用同一文本块代替。

预设属于项目共同层的全局设定；同一预设对H3、Wan3、SD2等引擎统一生效，各引擎只改变融合位置和格式，不改变预设值。执行依据：用户定制规则/命名预设与本次覆盖.md。


协同规则：涉及视觉风格、色卡、人物圣经或场景视觉规范时，读取 用户定制规则/VDE视觉导演引擎协同.md，并按 VDE → AI 视频项目模型分支的顺序处理。

