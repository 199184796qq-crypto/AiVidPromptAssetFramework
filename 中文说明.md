# 中文使用说明

## 这是什么

这是一个面向 AI 视频创作的 Agent 框架。它解决的问题是：在长时间、多项目、多视频、多工具和多智能体协作时，提示词、剧情、素材绑定和规则不会互相漂移。

## 基本层级

项目是总容器；一个项目可以有多个视频。每个视频拥有独立剧情、提示词版本、审核状态和素材关系。分镜是可选内容，只有实际生成过才建立记录。

项目共同层保存剧情、人物、素材、预设、光源和声音等事实。H3、Seedance 2、Seedance 2.5 等工具各自保存独立提示词，不把不同工具的格式混在一起。

## Agent 工作方式

Agent 必须从 `先读我.md` 开始，继续读取适用规则和当前项目状态。用户说“Agent设定”才进入框架规则规划状态；普通视频任务不能修改全局规则。

规则文件分为硬规则和普通规则。硬规则只能读取；普通规则只有在 Agent 设定状态开启且用户明确要求时才可修改。新增规则、规则基线和回退都保留历史，不删除旧文件。

## 视频工作方式

开始视频时先确认项目地址，再确认新视频或续写。续写还要区分：

- 物理延续：使用 Motion Context/context latent 和重叠帧，设计首镜衔接。
- 逻辑延续：剧情接着发展，但使用新的开场镜头，不使用重叠帧。

提示词第一次先交中文审核，用户明确通过后再生成英文。素材上传顺序和完整提示词始终分成两个独立的可复制文本块。

## 剧情、分镜和提示词

每个 H3 或其他工具提示词都有对应剧情版本。修改提示词中的创作内容时同步修改剧情；只修改剧情时不自动修改提示词。用户可以使用“看剧情”“看分镜”“看 H3 提示词”等命令分别回看。

## 保存与协作

编辑中的视频使用编辑锁。保存后没有新改动时，不重复写盘；有新改动时，离开当前编辑状态前自动保存并核验。切换项目、回看其他记录、换 Agent 或换机器前，先保存当前状态。

不同 Agent 使用同一个技能包和项目目录作为事实来源。接手前读取状态，交出前保存并写交接记录；发生冲突时保留两个版本，交由用户选择。

## 推荐命令

`开启 Agent 设定`

`列出新增规则`

`恢复最初规则`

`回退到规则R003以前`

`列出历史`

`当前项目`

`其他项目`

`看剧情`

`看分镜`

`看 Seedance2.5 提示词`
