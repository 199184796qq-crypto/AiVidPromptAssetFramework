# 画面可见文字固定格式

状态：R021，用户在 Agent 设定状态明确要求。

招牌、字幕、标签、横幅、霓虹字等画面中可见的文字，生成英文提示词时必须使用英文双引号包住，并保留用户指定的原文和标点。例如：`A red neon sign reading "营业中" glows above the doorway.`

不得擅自翻译、改写、删减或替换引号内的可见文字。没有明确文字内容时，不虚构具体字样；只描述文字载体、位置、材质和可见性。字幕是否生成仍服从本次字幕开关和用户确认，不能因本规则自动添加字幕。
