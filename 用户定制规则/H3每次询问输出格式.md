# H3 每次询问输出格式

状态：R017，用户明确选择每次都询问。适用于 H3 提示词生成，与是否上传素材无关。

故事获准且目标模型确定为 H3 后，在生成提示词正文前单独询问本次格式。列出可选择格式，将多参 Ref2VA 排在首位并标为推荐；推荐不等于选择，未得到用户回答不得自动按多参生成，也不得由素材有无自动选择模式。每次只问格式这一项。

选项内容：多参 Ref2VA（推荐）；纯文生 T2VA；首帧 I2VA；首尾帧 FL2VA；尾帧 L2VA。按当前界面提供简短可选答案，接受用户自然语言指定。基础关键帧模式需要明确关键帧条件；缺少时说明并补齐，不冒称已满足。

每次新生成、重新生成或改稿后输出完整 H3 提示词，均确认本次格式；不能继承旧视频、预设或上一轮格式跳过。本次请求已明确格式时回显确认即可，不要求重复回答。一次生成准备中的分轮讨论不重复询问已确认的同一次选择。仅回看原稿、解释规则或讨论局部内容不触发生成询问。

多参选择与素材上传状态分开记录。没有实际参考素材时仍可按用户所选多参六段结构组织文本，不虚构图片、音频、视频已上传，不创建没有定义的引用标签。用户明确要求假设素材已上传的模拟任务，按本次明确假设建立占位与绑定，但不得登记为真实上传。输出格式选择不代表平台实际生成接口已满足输入条件；本规则是用户的写作格式约定，不冒称官方推荐无素材使用 Ref2VA。

格式确认后读取冻结官方 Skill 所指的对应指南。多参依次使用 subject_definitions、summary、retention_analysis、detailed_description、overall_soundscape、non_diegetic_music；基础模式按对应官方指南及框架已有交付适配执行。中文先审、全局与本场灯光位置、素材顺序和声音规则持续有效。官方原文件不修改。

保存每次输出对应的格式、用户选择依据和素材实际或模拟状态，关联本视频提示词版本。辅助技能不得代替用户决定格式。本规则在生成正文前执行，不受提示词优先交付规则跳过。